You are working in repository via-decide/VIA on branch main.

MISSION
Implement a core backend module called via-blockchain-notary to provide cryptographic proof of origin and authenticity for all agent-generated content. 1. Create a new directory src/core/security/blockchain-notary/. 2. Create notary-config.json defining the target ledger (e.g., an EVM-compatible L2 or a high-throughput chain like Solana), RPC endpoints, batching intervals (e.g., anchor every 1000 messages or 10 minutes), and the Merkle Tree depth. 3. Implement NotaryProvider.js (or .ts). This module must capture agent outputs (text, media hashes, or decision logs) from the EventBus. 4. Build a Merkle Tree aggregator: Instead of writing every interaction to the blockchain (which is too slow and expensive), aggregate hashes of agent outputs into a Merkle Tree and only post the Merkle Root to the ledger. 5. Implement a getProof(contentId) method that retrieves the specific Merkle path and the on-chain transaction hash, allowing any third party to verify that a specific piece of data was indeed generated by the VIA swarm at a specific timestamp. 6. Integrate with AsyncDbSync: Store the local Merkle proofs and transaction receipts in the primary database for instant verification without re-querying the blockchain.

CONSTRAINTS
Do NOT use heavy, monolithic Web3 libraries like ethers.js or web3.js if they significantly increase worker thread memory footprint. You must use minimalist JSON-RPC calls over HTTPS/WebSocket. The anchoring process must be completely asynchronous; if the blockchain network is congested or an RPC node is down, the system must buffer hashes locally and retry without interrupting the real-time agent swarm.

PROCESS (MANDATORY)
1. Read README.md and AGENTS.md before editing.
2. Audit architecture before coding. Summarize current behavior.
3. Preserve unrelated working code. Prefer additive modular changes.
4. Implement the smallest safe change set for the stated goal.
5. Run validation commands and fix discovered issues.
6. Self-review for regressions, missing env wiring, and docs drift.
7. Return complete final file contents for every modified or created file.

REPO AUDIT CONTEXT
- Description: 
- Primary language: HTML
- README snippet:
not found

- AGENTS snippet:
not found


SOP: PRE-MODIFICATION PROTOCOL (MANDATORY)
1. Adherence to Instructions: No deviations without explicit user approval.
2. Mandatory Clarification: Immediately ask if instructions are ambiguous or incomplete.
3. Proposal First: Always propose optimizations or fixes before implementing them.
4. Scope Discipline: Do not add unrequested features or modify unrelated code.
5. Vulnerability Check: Immediately flag and explain security risks.

OUTPUT REQUIREMENTS
- Include: implementation summary, checks run, risks, rollback notes.
- Generate branch + PR package.
- Keep prompts deterministic and preservation-first.